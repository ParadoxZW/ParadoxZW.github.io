<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Zen Time</title>
  
  
  <link href="https://www.zenwill.top/atom.xml" rel="self"/>
  
  <link href="https://www.zenwill.top/"/>
  <updated>2021-07-05T10:36:58.450Z</updated>
  <id>https://www.zenwill.top/</id>
  
  <author>
    <name>shaw</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>VIM杂记(一)</title>
    <link href="https://www.zenwill.top/2020/03/09/VIM%E6%9D%82%E8%AE%B0-%E4%B8%80/"/>
    <id>https://www.zenwill.top/2020/03/09/VIM%E6%9D%82%E8%AE%B0-%E4%B8%80/</id>
    <published>2020-03-09T17:23:18.000Z</published>
    <updated>2021-07-05T10:36:58.450Z</updated>
    
    <content type="html"><![CDATA[<h1 id="写在前面"><a href="#写在前面" class="headerlink" title="写在前面"></a>写在前面</h1><p>每次都觉得我的vim走上正轨了</p><p>three months later…</p><p>卧槽 这什么bug？这个插件怎么用来着？那个命令怎么写来着？</p><p>three days later…</p><p>“我又好了！”</p><p>为了再次避免这种情况，打算把每次学到东西记录下来。查博客总比查百度高效一些。由于只是记录当前学到的东西，<br>又因为认识的片面性，<br>所以可能会有错误，反正后面发现是错误了，再在后续的杂记里更正就是了。</p><h1 id="1-1"><a href="#1-1" class="headerlink" title="1.1"></a>1.1</h1><h2 id="关于Vundle"><a href="#关于Vundle" class="headerlink" title="关于Vundle"></a>关于Vundle</h2><p>使用<code>Vundle</code>插件进行Vim插件的统一管理，包括安装、卸载等功能。 一般单独开一个文件<code>.vimrc.bundles</code>列出所要加载的插件，在<code>.vimrc</code>里需要载入该文件。 在<code>.vimrc.bundles</code>中列出所需插件之前，需要先将<code>Vundle</code>的代码包加入vim的运行时路径：</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">set rtp+&#x3D;~&#x2F;.vim&#x2F;bundle&#x2F;Vundle.vim&#x2F;</span><br></pre></td></tr></table></figure><p>其中<code>rtp</code>应该是runtime path的意思，vim有自己的运行时来解析执行vimscript脚本，想要调用预先定义在文件中的函数，自然需要将这些文件地址加入运行时地址列表。我猜后面其他插件的文件路径都是由Vundle负责加入<code>rtp</code>的。</p><p>在下面这样的代码块中加入所需要使用的插件</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">call vundle#begin()</span><br><span class="line">Plugin &#39;VundleVim&#x2F;Vundle.vim&#39;</span><br><span class="line">Plugin &#39;xxx&#x2F;xxxxx&#39;</span><br><span class="line">...</span><br><span class="line">call vundle#end()</span><br></pre></td></tr></table></figure><p>注意Vundle必须是第一个列入的插件。<code>call</code>是vimscript中的函数调用，<code>#</code>类似python或java中的<code>.</code>。如果不打算使用某插件可以注释掉对应的<code>Plugin</code>那一行，而不需要卸载。</p><h2 id="安装插件"><a href="#安装插件" class="headerlink" title="安装插件"></a>安装插件</h2><p>在<code>.vimrc.bundles</code>加入<code>Plugin xxx/xxxxxxx</code>，保存文件后在vim下执行</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">:PluginInstall</span><br></pre></td></tr></table></figure><h2 id="卸载插件"><a href="#卸载插件" class="headerlink" title="卸载插件"></a>卸载插件</h2><p>在<code>.vimrc.bundles</code>注释<code>Plugin xxx/xxxxxxx</code>，保存文件后在vim下执行</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">:PluginClean</span><br></pre></td></tr></table></figure><h1 id="1-2"><a href="#1-2" class="headerlink" title="1.2"></a>1.2</h1><ul><li><code>ultisnips</code>是一个snippets的引擎，<code>vim-snippets</code>里面有很多常用的脚本。这两个插件需要配合使用。</li><li>发现自己的<code>ultisnips</code>有bug，然后我重装了，然后出现更大的bug，查了半天是github上的最新版本的代码支持python3了，但是我的vim只支持python2, 于是手动下了github上之前版本的代码放入<code>.vim/Bundle</code>文件夹。 但是还是有bug，发现我的<code>Enter</code>键没法回车了，查了半天bug，甚至阅读了<code>ultisnips</code>的部分代码（主要是<code>./autoload/UltiSnips/map_keys.vim</code>和<code>./autoload/UltiSnips.vim</code>），经过各种调试发现是，somehow，我的vim在select模式下<code>&lt;c-m&gt;</code>和<code>&lt;cr&gt;</code>是捆绑的，我配置了用<code>&lt;c-m&gt;</code>来展开，结果就导致了<code>&lt;cr&gt;</code>也映射到了展开snippets的命令上。。。TAT。最后试了几个键发现<code>c-l</code>是好用的，也顺手。</li><li>另外，我观察到在打开vim的瞬间，ultisnips会先跑两个样例，如果这两个样例有问题，就会在打开vim的时候报错，一般情况下用户是察觉不到这个过程的。</li><li><code>inc</code>、<code>incc</code>、<code>mainn</code>、<code>fori</code>都是c语言中常用的snippets</li><li>在我的配置中<code>&lt;c-l&gt;</code>可以进行展开或者跳到下一个placeholder，<code>&lt;c-k&gt;</code>回到上一个placeholder。</li><li><code>:UltiSnipsEdit</code>可以查看对应当前编辑文件的snippet配置文件 </li></ul><h1 id="1-3"><a href="#1-3" class="headerlink" title="1.3"></a>1.3</h1><p>在vim插件的代码中可以通过<code>verbose map **</code>和<code>echo</code>进行调试。<code>:map</code>、<code>:snoremap</code>等可以查看对应模式下的按键映射。关于按键映射的知识点，可参考<a class="link"   href="https://blog.csdn.net/u012291393/article/details/78650512" >https://blog.csdn.net/u012291393/article/details/78650512<i class="fas fa-external-link-alt"></i></a></p><h1 id="1-4"><a href="#1-4" class="headerlink" title="1.4"></a>1.4</h1><p>多行缩进</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">:10,20&gt;</span><br><span class="line">:12,13&lt;</span><br></pre></td></tr></table></figure><p>跑Python代码</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">:py import sys; print(sys.version)</span><br></pre></td></tr></table></figure><p>多行注释: ctrl+v进入列编辑模式，在编辑模式中，在行间上下移动光标，选择需要编辑哪些行的对应的列，之后使用I（大写）进入列插入。 插入注释符，按Esc退出列编辑模式即可实现多行注释。</p><p>删除注释：按v,进入visual模式,横向选中列的个数(如”/ /“注释符号,需要选中两列),再按ctrl+v 进入列编辑模式,向下或向上移动光标,选中注释部分,然后按d, 就会删除注释符号。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;写在前面&quot;&gt;&lt;a href=&quot;#写在前面&quot; class=&quot;headerlink&quot; title=&quot;写在前面&quot;&gt;&lt;/a&gt;写在前面&lt;/h1&gt;&lt;p&gt;每次都觉得我的vim走上正轨了&lt;/p&gt;
&lt;p&gt;three months later…&lt;/p&gt;
&lt;p&gt;卧槽 这什么bug？这个</summary>
      
    
    
    
    
    <category term="VIM" scheme="https://www.zenwill.top/tags/VIM/"/>
    
  </entry>
  
  <entry>
    <title>在Python中写lambda calculus</title>
    <link href="https://www.zenwill.top/2019/09/08/%E5%9C%A8Python%E4%B8%AD%E5%86%99lambda-calculus/"/>
    <id>https://www.zenwill.top/2019/09/08/%E5%9C%A8Python%E4%B8%AD%E5%86%99lambda-calculus/</id>
    <published>2019-09-08T21:32:05.000Z</published>
    <updated>2021-07-05T10:36:58.450Z</updated>
    
    <content type="html"><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>可能会有一个系列，专门记录自己研究过的奇技淫巧。 虽然有时候我们会觉得奇技淫巧是花里胡哨，华而不实的，但是当自己写出来的时候，就还是“真香”。 况且奇技淫巧之中其实蕴含着优美与真理，主要看你怎么去理解它们了。 这次的问题来源于我在网上看到的一个用lambda表达式写递归的Python语句</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">factorial = (<span class="keyword">lambda</span> a:<span class="keyword">lambda</span> v:a(a)(v))(<span class="keyword">lambda</span> s: <span class="keyword">lambda</span> x:<span class="number">1</span> <span class="keyword">if</span> x==<span class="number">0</span> <span class="keyword">else</span> x*s(s)(x-<span class="number">1</span>))  <span class="comment"># 实现了递归</span></span><br><span class="line"><span class="built_in">print</span>(factorial(<span class="number">5</span>)) <span class="comment"># 120</span></span><br></pre></td></tr></table></figure><p>上面这个求阶乘的表达式我看了快半个小时才觉得自己大概是看懂了。好奇心驱使我开始往下挖坑，想看怎样能更好地理解这种写法。 因为学过Haskell，所以我知道这里可能涉及到了<code>lambda calculus</code>和 <code>Y combinator</code>的内容。 之前看过一点这个topic，但是也没有太深入研究。</p><h1 id="先扯扯lambda-calculus"><a href="#先扯扯lambda-calculus" class="headerlink" title="先扯扯lambda calculus"></a>先扯扯lambda calculus</h1><p>lambda calculus可以理解为一个数学系统，其中最基本的元素或者说研究对象是<strong>函数</strong>, 通过函数来构造出我们做计算所需要的所有“材料”（比如自然数，四则运算符等）。 其实这有点像集合论，通过集合论可以构造出现代数学的几乎所有内容，比如皮亚诺公理就是用集合（集合套空集，集合套集合的骚操作）构造自然数。 说回lambda calculus，lambda calculus中的<strong>函数</strong>具有一些特征，这些函数是单参数的，它们能且只能接受一个函数作为参数； 当我们把一个函数作用于另一个函数（后者作为前者的参数），得到的返回值也是一个单参数函数。 这样整个系统其实就封闭了，我们可以在里面搭积木而不再需要其他任何东西了。 然后看看notation:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">λ x. E   <span class="comment"># 这表示一个函数，函数参数为x，E为函数所作运算的具体表达式</span></span><br><span class="line">M N      <span class="comment"># 将函数M作用于函数N，函数作用是可以省略括号的</span></span><br></pre></td></tr></table></figure><p>第一个式子中的<code>E</code>可以包含<code>x</code>，也可以不包含，<code>E</code>就相当于一个<code>f(x)</code>， 如果<code>f(x)</code>的表达式中不包含<code>x</code>，就意味着函数的输出和输入无关。 lambda calculus中所有合法的表达式的值都是单参数函数。 另外函数是左结合的，即<code>X Y Z = (X Y) Z =(X(Y))(Z)</code>，所以<code>X (Y Z)</code>这个式子里的括号是不能去的。 函数式编程中的lambda表达式就是借鉴了<code>λ x. E</code>这样的写法，例如我们可以在Python中这样定义函数（Python借鉴了函数式编程）:</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">f = <span class="keyword">lambda</span> x: x + x  <span class="comment"># f(2) 的值是4</span></span><br><span class="line">g = <span class="keyword">lambda</span> x, y: x * y  <span class="comment"># f(2, 3) 的值是6</span></span><br></pre></td></tr></table></figure><p>但是<code>λ x. E</code>和编程语言中的lambda表达式其实是有区别的，首先语法上lambda表达式并没有限制参数的个数（对于有柯里化特性的语言写两个参数在语义上还是可以理解成单参数函数的，例如Haskell），然后就是参数的类型不一定是函数，所创建的函数的返回值类型也不一定是函数。<strong>注意，lambda表达式的返回值是函数，但是这个函数的返回值，即它的输出是要看它接收参数后的运算结果，在lambda calculus中这种运算结果的值仍然是函数</strong>。 然后看一些基本的恒等式</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">λ x. F x = F</span><br><span class="line">(λ x. E) T = E[x:=T]</span><br><span class="line">λ x. E[x] = λ y. E[y]</span><br></pre></td></tr></table></figure><p>第二个式子的右侧是值把表达式<code>E</code>中的所有<code>x</code>都替换成<code>T</code>，第三个式子是说把参数名字换掉不改变它所代表的函数。 这三个式子的成立都很显然，应用它们做一些数学证明的时候可能从左推到右，也可能从右推到左。<br>然后我们看看，lambda calculus中是如何定义自然数的</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">0 = λ f. λ x. x</span><br><span class="line">1 = λ f. λ x. f x</span><br><span class="line">2 = λ f. λ x. f (f x)</span><br><span class="line">3 = λ f. λ x. f (f (f x))</span><br></pre></td></tr></table></figure><p>我们称这样定义的自然数为Church数（不是教堂，是lambda calculus的发明者的名字）。 对这种定义的一种粗略的理解是，自然数<code>n</code>是这样一个函数，它接收任意一个函数<code>f</code>，返回了一个函数<code>A</code>，这个函数<code>A</code>在接收任意一个函数<code>x</code>,返回一个函数<code>B</code>，这个<code>B</code>的值等价于<code>f</code>作用于<code>x</code>，连续作用<code>n</code>次的结果。 但是，为什么要这样定义呢？后面我们还要定义各种运算，定义完的结果就是<code>1+1=2</code>这样的等式的成立是可以在lambda calculus框架下进行证明的。所以为什么这样定义，因为自洽。 虽然可以证明某某等式的成立，但是lambda calculus中的表达式往往又“臭”又长，用人脑去解析它们真的十分费力，所以我就想能不能用Python对表达式进行计算，然后<code>print</code>出一个函数所代表的Church数的值。</p><h1 id="Python模拟lambda-calculus"><a href="#Python模拟lambda-calculus" class="headerlink" title="Python模拟lambda calculus"></a>Python模拟lambda calculus</h1><p>我希望的模拟不是字符串的规约推导，而是用Python的lambda表达式去比较真实的模拟，同时尽可能的保持lambda calculus的特性，也就是所有表达式或名字的值都是函数类型。 所以我写了下面一段十分神奇的代码：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># interpreting and visualize</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">x_</span>():</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">f_</span>(<span class="params">x=<span class="literal">None</span></span>):</span></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">lambda</span> : <span class="number">1</span> + x()</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">interpret</span>(<span class="params">f</span>):</span></span><br><span class="line">    <span class="string">&#x27;&#x27;&#x27;查看一个函数的自然数值，具体见下文&#x27;&#x27;&#x27;</span></span><br><span class="line">    <span class="built_in">print</span>(f(f_)(x_)())</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">predicate</span>(<span class="params">f</span>):</span></span><br><span class="line">    <span class="string">&#x27;&#x27;&#x27;查看一个函数的bool值，具体见下文&#x27;&#x27;&#x27;</span></span><br><span class="line">    <span class="keyword">if</span> f(f_)(x_)() == <span class="number">0</span>:</span><br><span class="line">        <span class="built_in">print</span>(<span class="literal">False</span>)</span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        <span class="built_in">print</span>(<span class="literal">True</span>)</span><br></pre></td></tr></table></figure><p>这段代码几乎满足了我的所有要求，唯一的不足已经是语言本身的不足了（Python不支持惰性求值）。 具体的使用看后面的例子，后面例子里的所有代码（除了调用interpret或predicate）都是在对函数进行运算了，输入、输出和各种变量什么的全都是单变量函数。</p><h2 id="自然数"><a href="#自然数" class="headerlink" title="自然数"></a>自然数</h2><p>我们现在开始做一些定义和测试。 根据上面提到的Church数的定义，我们可以这样写</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">Church_0 = <span class="keyword">lambda</span> f: <span class="keyword">lambda</span> x: x</span><br><span class="line">Church_1 = <span class="keyword">lambda</span> f: <span class="keyword">lambda</span> x: f(x)</span><br><span class="line">Church_2 = <span class="keyword">lambda</span> f: <span class="keyword">lambda</span> x: f(f(x))</span><br><span class="line">Church_3 = <span class="keyword">lambda</span> f: <span class="keyword">lambda</span> x: f(f(f(x)))</span><br></pre></td></tr></table></figure><p>代码定义完全照抄上面的数学定义。 然后我们<code>print</code>它们的值看看</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">interpret(Church_0)  <span class="comment"># 0</span></span><br><span class="line">interpret(Church_2)  <span class="comment"># 2</span></span><br><span class="line">interpret(Church_3)  <span class="comment"># 3</span></span><br></pre></td></tr></table></figure><p>后面的注释就是打印的结果，十分完美。</p><h2 id="运算符"><a href="#运算符" class="headerlink" title="运算符"></a>运算符</h2><p>然后我们再利用这套代码定义一些运算符看看。 下面是我们在这篇文章中将要使用的运算符的数学定义</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">SUCC = λ n. λ f. λ x. f (n f x)  <span class="comment"># 加1运算</span></span><br><span class="line">PLUS = λ m. λ n. λ f. λ x. m f (n f x)  <span class="comment"># 求和运算</span></span><br><span class="line">MULT = λ m. λ n. m (PLUS n) 0  <span class="comment"># 求积运算</span></span><br><span class="line">PRED = λ n. λ f. λ x. n (λ g. λ h. h (g f)) (λ u. x) (λ u. u) <span class="comment"># 减一运算</span></span><br></pre></td></tr></table></figure><p>这里先讲一下二元运算符<code>PLUS</code>和<code>MULT</code>，<code>PLUS x y</code> 这个式子表示 <code>x + y</code>，如果<code>x</code>是Church数<code>1</code>，<code>y</code>是Church数<code>2</code>，<code>PLUS x y</code>的返回值就是Church数<code>3</code>。 但是<code>PLUS</code>仍然是一个单参数函数，它接收函数<code>x</code>然后返回 <code>PLUS x</code>，<code>PLUS x</code>是一个函数，这个函数接收一个函数<code>y</code>然后返回<code>x + y</code>，即<code>PLUS x y = (PLUS x) y</code>，这就是所谓的柯里化。 <code>MULT</code>运算同理。 下面我们上代码</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">SUCC = <span class="keyword">lambda</span> n: <span class="keyword">lambda</span> f: <span class="keyword">lambda</span> x: f(n(f)(x))</span><br><span class="line">interpret(SUCC(Church_3)) <span class="comment"># 4</span></span><br><span class="line"></span><br><span class="line">PLUS = <span class="keyword">lambda</span> m: <span class="keyword">lambda</span> n: <span class="keyword">lambda</span> f: <span class="keyword">lambda</span> x: m(f)(n(f)(x))</span><br><span class="line">interpret(PLUS(Church_3)(Church_2)) <span class="comment"># 5</span></span><br><span class="line"></span><br><span class="line">MULT = <span class="keyword">lambda</span> m: <span class="keyword">lambda</span> n: m(PLUS(n))(Church_0)</span><br><span class="line">interpret(MULT(Church_2)(Church_3)) <span class="comment"># 6</span></span><br><span class="line"></span><br><span class="line">PRED = <span class="keyword">lambda</span> n: <span class="keyword">lambda</span> f: <span class="keyword">lambda</span> x: n(<span class="keyword">lambda</span> g: <span class="keyword">lambda</span> h: h(g(f)))(<span class="keyword">lambda</span> u: x)(<span class="keyword">lambda</span> u: u)</span><br><span class="line">interpret(PRED(Church_3)) <span class="comment"># 2</span></span><br></pre></td></tr></table></figure><p>虽然这些定义看起来很吓人，但是结果都十分正确。 我们试一个更复杂的</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">interpret(PRED(MULT(Church_2)(Church_3))) <span class="comment"># 5</span></span><br></pre></td></tr></table></figure><p>也是正确的。 当我照抄数学定义然后成功<code>print</code>出正确的结果的时候，感到惊喜且惊奇。 我们只是搭好了一个简单的地基，然后就看着这些建立在地基之上的复杂事物表现出我们所期待的行为。 这就是数学的魅力吧。</p><h2 id="逻辑运算"><a href="#逻辑运算" class="headerlink" title="逻辑运算"></a>逻辑运算</h2><p>上文说lambda calculus可以定义出我们用来做计算需要的所有东西，这里的计算就包括逻辑计算。和C语言类似，我们定义表示0的函数为<code>False</code>，而其他的函数为<code>True</code>。 看一下具体的数学定义</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">TRUE = λ u. λ v. u</span><br><span class="line">FALSE = λ u. λ v. v</span><br></pre></td></tr></table></figure><p>Python的代码定义如下</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">Church_True = <span class="keyword">lambda</span> u: <span class="keyword">lambda</span> v: u</span><br><span class="line">Church_False = <span class="keyword">lambda</span> u: <span class="keyword">lambda</span> v: v</span><br></pre></td></tr></table></figure><p>根据我们之前提过的恒等式中的第三条，我们知道<code>False</code>和<code>0</code>在lambda calculus中是同一个函数。所以除了<code>interpret</code>，我写了另一个函数<code>predicate</code>，它把函数看作是一个逻辑断言，并打印出其真值。我们来测试一下</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">predicate(Church_False)  <span class="comment"># False</span></span><br><span class="line">predicate(Church_True)   <span class="comment"># True</span></span><br><span class="line">predicate(Church_0)      <span class="comment"># False</span></span><br><span class="line">predicate(Church_2)      <span class="comment"># True</span></span><br></pre></td></tr></table></figure><p>与我们预期的一样，除了<code>0</code>, 我们之前定义的Church数都被认为是逻辑<code>True</code>。 这样我们就在lambda calculus的背景下构造了一个布尔代数系统，我们可以定义各种逻辑运算符和布尔函数</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># AND = λ p.λ q.p q p</span></span><br><span class="line">AND = <span class="keyword">lambda</span> p: <span class="keyword">lambda</span> q: p(q)(p)</span><br><span class="line"><span class="comment"># OR = λ p.λ q.p p q</span></span><br><span class="line">OR = <span class="keyword">lambda</span> p: <span class="keyword">lambda</span> q: p(p)(q)</span><br><span class="line"><span class="comment"># NOT = λ p.p FALSE TRUE</span></span><br><span class="line">NOT = <span class="keyword">lambda</span> p: p(Church_False)(Church_True)</span><br><span class="line">predicate(NOT(OR(Church_False)(Church_True)))  <span class="comment"># False</span></span><br></pre></td></tr></table></figure><p>基于<code>0</code>和<code>False</code>的一致性，我们可以定义一个判断输入是否为<code>0</code>的函数，当且仅当输入为<code>0</code>时返回<code>True</code>：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 数学定义： ISZERO = λ n. n (λ x. FALSE) TRUE</span></span><br><span class="line">ISZERO = <span class="keyword">lambda</span> n: n(<span class="keyword">lambda</span> x: Church_False)(Church_True)</span><br><span class="line">predicate(ISZERO(Church_0))  <span class="comment"># True</span></span><br><span class="line">predicate(ISZERO(Church_3))  <span class="comment"># False</span></span><br></pre></td></tr></table></figure><h2 id="递归"><a href="#递归" class="headerlink" title="递归"></a>递归</h2><p>现在到了最激动人心的part。 我们以阶乘函数为例来进行探讨，首先来看看在Haskell中我们如何用递归的方式定义阶乘函数（用Haskell举例是因为Haskell写出来的阶乘函数特别接近我们用自然语言进行的递归算法的描述，同时又可以比较形式化）</p><figure class="highlight haskell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="title">factorial</span> <span class="number">0</span> = <span class="number">1</span></span><br><span class="line"><span class="title">factorial</span> n = n * factorial (n<span class="number">-1</span>)</span><br></pre></td></tr></table></figure><p>从中我们看到了递归函数的一个重要特征，就是函数名会出现在函数定义里头。 所以乍看之下我们会觉得lambda表达式无法表达递归函数，如果硬着头皮，我们会写出像下面这样的式子</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">F = λ x. F (PRED x)</span><br></pre></td></tr></table></figure><p>这个式子存在两个问题，一是在数学中我们一般不允许循环定义，你还没有定义完成F是啥，就还不能使用F，另一方面，即使语法上允许我们这么表达一个函数，这个函数也有可能是个没有尽头的死循环（<strong>注意没有输出就不是函数了</strong>）。 不过Python中的lambda表达式允许我们进行“循环定义”，因为与数学不同，Python中的函数是先定义再调用的，只有在调用的时候（也就是你在函数名后面加<code>()</code>的时候，如<code>fun()</code>）才会真正去执行一个函数的代码，而定义的时候只是将一个函数名和一串代码绑定起来，并不会去分析这段代码究竟干了什么。所以我们可以像下面这样定义</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">FACT_ = <span class="keyword">lambda</span> n: n(<span class="keyword">lambda</span> u: MULT(n)(FACT_(PRED(n))))(Church_1)</span><br><span class="line">interpret(FACT_(Church_0))  <span class="comment"># 1</span></span><br><span class="line">interpret(FACT_(Church_3))  <span class="comment"># 6</span></span><br></pre></td></tr></table></figure><p>这个<code>FACT_</code>功能良好。 有两点十分有趣，我们看到<code>0</code>的阶乘十分自然的等于<code>1</code>，说明数学家们当初规定<code>0! = 1</code>不是随便规定的，是有一定的道理的。 另一点就是<code>FACT_</code>并没有陷入死循环，这一点看似很奇怪，因为我们似乎并没有像传统递归写法那样设置临界条件，那这个循环函数调用是如何停下来的呢？ 这里就涉及到Church<code>0</code>的定义的奥妙之处了</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">0 = λ f. λ x. x</span><br></pre></td></tr></table></figure><p>实际上函数运算到最后会出现<code>0(MULT n (PRED n))(1)</code>这样的情况，这个时候按<code>0</code>的定义，他会直接忽略传进去的<code>(MULT n (PRED n))</code>而直接返回函数<code>1</code>。 <code>(MULT n (PRED n))</code>只出现在参数里但不再出现在函数表达式里，所以也就不会再去往下计算它了。<br>说到这里，阶乘函数的实现还是依赖于Python的语言特性，而不是纯数学的lambda calculus的定义，而且看起来lambda calculus没法使用还没定义好的名字，所以似乎也写不出递归函数。 但是别慌，数学家们还是研究出来了，lambda calculus中有个叫<code>Y combinator</code>（Y组合子）的东西为lambda calculus提供了递归的实现手段</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Y = λ f. (λ x. f (x x)) (λ x. f (x x))</span><br></pre></td></tr></table></figure><p>其实并不神秘，稍作思考我们就可以得到下面这个结论</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Y F = F(F(F(F(. . .)))) <span class="comment"># 无限循环调用</span></span><br></pre></td></tr></table></figure><p>然后我尝试在Python中实现<code>Y combinator</code></p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">Y = <span class="keyword">lambda</span> f: (<span class="keyword">lambda</span> x: f(x(x)))(<span class="keyword">lambda</span> x: f(x(x)))</span><br><span class="line">F = <span class="keyword">lambda</span> g: <span class="keyword">lambda</span> n: n(<span class="keyword">lambda</span> u: MULT(n)(g(PRED(n))))(Church_1)</span><br><span class="line">FACT = Y(F)</span><br><span class="line">interpret(FACT(Church_3))</span><br></pre></td></tr></table></figure><p>然后不幸发生了，到第三行Python报错了，说是递归爆栈了，也就是陷入了死循环。这是因为Python不是惰性求值的，当我执行<code>FACT = Y(F)</code>的时候，解释器就会去执行<code>Y</code>函数的计算，按照定义自然会出现无限调用的情况。 lambda calculus所希望的是，你先别去计算<code>Y(F)</code>的返回值究竟是个什么函数，等这个函数作用了<code>Church_3</code>再去实施计算，到时候<code>Church_0</code>就会负责把死循环截断。所以很多函数式编程语言都会设计成惰性求值来增强语言的表达能力，避免不必要的死循环。<br>那么路走到这里，就走到尽头了吗？并没有，回顾我们开篇提到的阶乘函数的实现，它也是用lambda表达式写的，也没有使用未定义的名字，于是我仿照它不使用<code>Y combinator</code>，重新定义了阶乘函数</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">T = λ f. λ x. f f x</span><br><span class="line">G = λ g. λ n. n (λ u. MULT n (g g (PRED n))) 1</span><br><span class="line">FACT = T G</span><br></pre></td></tr></table></figure><p>然后使用Python实现并测试</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">T = <span class="keyword">lambda</span> f: <span class="keyword">lambda</span> x: f(f)(x)</span><br><span class="line">G = <span class="keyword">lambda</span> g: <span class="keyword">lambda</span> n: n(<span class="keyword">lambda</span> u: MULT(n)(g(g)(PRED(n))))(Church_1)</span><br><span class="line">FACT = T(G)</span><br><span class="line">interpret(FACT(PLUS(Church_3)(Church_2)))  <span class="comment"># 120</span></span><br></pre></td></tr></table></figure><p>成功了。 一个合理的解释是，在数学上两个<code>FACT</code>是同一个函数，只是我们通过数学上的变换调整了函数的组织方式，从而改变了其中的运算顺序，避免了因为不支持惰性求值而造成的问题。 这个猜想的正确性需要需要通过数学证明来保证，也就是证明<code>Y F = T G</code>。</p><h3 id="证明"><a href="#证明" class="headerlink" title="证明"></a><em>证明</em></h3><p>我们观察到<code>G</code>和<code>F</code>的主要区别就在于<code>g (PRED n)</code>和<code>g g (PRED n)</code>，所以我们的入手点也是这里，先尝试把两个<code>g</code>变成一个函数来表示</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">G = λ g. (λ h. λ n. n (λ u. MULT n (h (PRED n))) 1)(g g)</span><br><span class="line">  = λ g. F (g g)</span><br></pre></td></tr></table></figure><p>所以有</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">T G = (λ f. λ x. f f x)(λ g. F (g g))</span><br><span class="line">    = λ x. (λ g. F (g g)) (λ g. F (g g)) x</span><br><span class="line">    = (λ g. F (g g)) (λ g. F (g g))</span><br><span class="line">    = (λ f. (λ g. f (g g)) (λ g. f (g g))) F</span><br><span class="line">    = Y F</span><br></pre></td></tr></table></figure><p>证明成功。</p><h1 id="结尾"><a href="#结尾" class="headerlink" title="结尾"></a>结尾</h1><p>这次的探究启发了我们写递归函数的方式，并不是只有自己定义（中出现）自己才能实现递归。当然我不会在Python中这样写递归函数，因为可读性太差，对于不了解函数式编程的人，这样的代码非常不友好。即使在Haskell中，我也不会这样写，因为Haskell中有更优美的递归写法。但是lambda calculus中对函数的变换、组合和使用的精髓是值得我们学习的，这有助于我在写函数式的Python语句或写Haskell时更清楚我能写什么，我能怎么写。</p><h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a><em>参考</em></h1><ol><li><a href="en.wikipedia.org/wiki/Lambda_calculus">Lambda_calculus</a></li><li><a href="blog.kaaass.net/archives/950">各语言Y组合子大比拼</a></li></ol>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h1&gt;&lt;p&gt;可能会有一个系列，专门记录自己研究过的奇技淫巧。 虽然有时候我们会觉得奇技淫巧是花里胡哨，华而不实的，但是当自己写出来的时候，就还是“真香”</summary>
      
    
    
    
    
    <category term="lambda calculus" scheme="https://www.zenwill.top/tags/lambda-calculus/"/>
    
    <category term="python" scheme="https://www.zenwill.top/tags/python/"/>
    
  </entry>
  
  <entry>
    <title>windows上利用docker安装MySQL</title>
    <link href="https://www.zenwill.top/2019/03/30/windows%E4%B8%8A%E5%88%A9%E7%94%A8docker%E5%AE%89%E8%A3%85Mysql/"/>
    <id>https://www.zenwill.top/2019/03/30/windows%E4%B8%8A%E5%88%A9%E7%94%A8docker%E5%AE%89%E8%A3%85Mysql/</id>
    <published>2019-03-30T20:59:55.000Z</published>
    <updated>2021-07-05T10:36:58.450Z</updated>
    
    <content type="html"><![CDATA[<h1 id="安装docker"><a href="#安装docker" class="headerlink" title="安装docker"></a>安装docker</h1><p>在Windows上安装Dockers Toolbox，<a class="link"   href="http://wiki.jikexueyuan.com/project/rust-primer/editors/before.html/" >下载链接<i class="fas fa-external-link-alt"></i></a>，<br>选择ce版本。安装完成后有三个软件，一个是VM Virtualbox，负责装载<code>docker-machine</code>虚拟机；一个Quickstart是一个虚拟终端，点击这个应用后会自动新建一个machine；另一个是一个GUI,基本不用。</p><h1 id="docker使用与配置"><a href="#docker使用与配置" class="headerlink" title="docker使用与配置"></a>docker使用与配置</h1><p>docker运行在一个Linux系统的虚拟机上，即machine，第一次点开Quickstart默认创建的machine的名字一般是<code>default</code>；在machine中可以新建和运行容器，容器可以理解为在原始machine的基础上叠加环境信息，并形成一个新的“虚拟机”，因此运行在同一个machine上的不同的容器之间是隔离的；<br>在Windows的cmd或者Powershell中可以通过<code>docker-machine</code>对虚拟机进行管理</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 创建名叫default的machine</span></span><br><span class="line">docker-machine create --driver=virtualbox default</span><br><span class="line"><span class="comment"># 停止machine</span></span><br><span class="line">docker-machine stop default <span class="comment"># 重启用restart，启动用start</span></span><br><span class="line"><span class="comment"># 删除一个machine</span></span><br><span class="line">docker-machine rm default</span><br><span class="line"><span class="comment"># 查看machine信息，包括其IP地址</span></span><br><span class="line">docker-machine ls</span><br></pre></td></tr></table></figure><p>通过<code>docker-machine</code>可以在Windows环境中获得machine的环境，从而无需登录machine就可以在<br>cmd中执行<code>docker</code>开头的指令（docker本是machine中的应用而不是在Windows中）。执行</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 创建名叫default的machine</span></span><br><span class="line">docker-machine env default <span class="comment">#根据输出执行命令</span></span><br></pre></td></tr></table></figure><p>即可获得环境，例如我们现在可以执行<code>docker version</code>。\<br>可以通过<code>docker-machine</code>登陆machine</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">docker-machine ssh default</span><br></pre></td></tr></table></figure><p>登陆后<code>sudu vi /var/lib/boot2docker/profile</code>,<code>在EXTRA_ARGS</code>中添加国内的镜像源</p><figure class="highlight plain"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">--registry-mirror https：&#x2F;&#x2F;registry.docker-cn.com</span><br></pre></td></tr></table></figure><p><code>exit</code>退出虚拟机，重启虚拟机，然后就可以开始拉镜像了。</p><h1 id="安装Mysql"><a href="#安装Mysql" class="headerlink" title="安装Mysql"></a>安装Mysql</h1><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 安装镜像</span></span><br><span class="line">docker pull mysql</span><br><span class="line"><span class="comment"># 新建容器</span></span><br><span class="line">docker run -d -p 3306:3306 --name mysql -e MYSQL_ROOT_PASSWORD=123 mysql:latest</span><br></pre></td></tr></table></figure><p>一些常用操作</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 查看镜像</span></span><br><span class="line">docker image ls</span><br><span class="line"><span class="comment"># 查看容器</span></span><br><span class="line">docker ps -a</span><br><span class="line"><span class="comment"># 暂停容器</span></span><br><span class="line">docker stop $(containerID)</span><br><span class="line"><span class="comment"># 启动容器</span></span><br><span class="line">docker start mysql <span class="comment"># 后面是容器的name</span></span><br><span class="line"><span class="comment"># 登陆容器</span></span><br><span class="line">docker <span class="built_in">exec</span> -it mysql bash</span><br></pre></td></tr></table></figure><h1 id="远程连接Mysql"><a href="#远程连接Mysql" class="headerlink" title="远程连接Mysql"></a>远程连接Mysql</h1><p>登陆容器后登陆MySQL</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 以root身份登陆，密码为123</span></span><br><span class="line">mysql -u root -p</span><br></pre></td></tr></table></figure><p>进入MySQL引擎</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">USER</span> <span class="string">&#x27;szw&#x27;</span>@<span class="string">&#x27;%&#x27;</span> IDENTIFIED <span class="keyword">WITH</span> mysql_native_password <span class="keyword">BY</span> <span class="string">&#x27;szw&#x27;</span>;</span><br><span class="line"><span class="keyword">GRANT</span> <span class="keyword">ALL</span> PRIVILEGES <span class="keyword">ON</span> <span class="operator">*</span>.<span class="operator">*</span> <span class="keyword">TO</span> <span class="string">&#x27;szw&#x27;</span>@<span class="string">&#x27;%&#x27;</span>;</span><br></pre></td></tr></table></figure><p>下载Navicat for MySQL，打开应用，点击连接，填写相关信息，IP为<code>default</code>虚拟机的IP，账号<br><code>szw</code>,密码<code>szw</code>，然后就正常连接了。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;安装docker&quot;&gt;&lt;a href=&quot;#安装docker&quot; class=&quot;headerlink&quot; title=&quot;安装docker&quot;&gt;&lt;/a&gt;安装docker&lt;/h1&gt;&lt;p&gt;在Windows上安装Dockers Toolbox，&lt;a class=&quot;link&quot;   h</summary>
      
    
    
    
    
    <category term="docker" scheme="https://www.zenwill.top/tags/docker/"/>
    
    <category term="MySQL" scheme="https://www.zenwill.top/tags/MySQL/"/>
    
  </entry>
  
  <entry>
    <title>Rust+VSCode从build到debug原创配置</title>
    <link href="https://www.zenwill.top/2018/07/16/Rust-VSCode%E4%BB%8Ebuild%E5%88%B0debug%E5%8E%9F%E5%88%9B%E9%85%8D%E7%BD%AE/"/>
    <id>https://www.zenwill.top/2018/07/16/Rust-VSCode%E4%BB%8Ebuild%E5%88%B0debug%E5%8E%9F%E5%88%9B%E9%85%8D%E7%BD%AE/</id>
    <published>2018-07-16T22:09:40.000Z</published>
    <updated>2021-07-05T10:36:58.450Z</updated>
    
    <content type="html"><![CDATA[<h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p>搞了整整一天的rust+vscode的开发环境，因为rust太新了，再加上Windows用户往往只是开发者中的小众，所以完整的、高质量的环境配置的教程、博客少的可怜，而完整的从编译到调试的配置教程几乎没有，所以我配置好的<code>.vscode</code>算是纯原创配置了，所以在这里分享一下，大家也可以少走一点弯路。</p><h1 id="前期准备"><a href="#前期准备" class="headerlink" title="前期准备"></a>前期准备</h1><p>网上教程还是有一些的，这里只讲一下要注意的细节</p><ul><li>rust安装： 按照官网的安装教程来就可以，Windows上默认安装的是<code>MSVC ABI</code>的版本（你电脑上有VS就可以正常安装，没有的话它会让你下载一个VC++的软件包），当然你也可以装<code>GNU ABI</code>版本，这决定了后面的debug用的是MSVC还是GDB。本文只介绍MSVC版本的配置。</li><li>工具安装： <code>racer</code>需要手动安装，前面还需要装个rust的源码。<a class="link"   href="http://wiki.jikexueyuan.com/project/rust-primer/editors/before.html/" >具体步骤参考此文<i class="fas fa-external-link-alt"></i></a>只要看<code>下载rust源码</code>和<code>racer</code>部分就好。剩下的部分在vscode里装插件的时候会提示你安装。</li></ul><h1 id="VSCode配置"><a href="#VSCode配置" class="headerlink" title="VSCode配置"></a>VSCode配置</h1><h2 id="vscode插件安装"><a href="#vscode插件安装" class="headerlink" title="vscode插件安装"></a>vscode插件安装</h2><ul><li><p>要进行debug，需要安装微软自家的插件<code>C/C++</code>和<code>Native Debug</code>插件。用VSCode写过c或c++的人应该都装了这两个插件。</p></li><li><p>先安装<code>Rust(rls)</code>再安装<code>Rusty Code</code>，过程中提示你装的工具都点确定就会帮你自动装好。然后<code>C:\Users\&lt;username&gt;\.rustup\toolchains</code>里应该就会有两个版本的工具链，<code>stable</code>和<code>nightly</code>。这些步骤完成后用VSCode写Rust，已经具备了代码补全、智能提示等基础功能了，另外在vscode里<code>ctrl+shift+p</code>再输入<code>cargo</code>可以看到vscode里已经有了cargo的命令及一些常用命令的快捷键。</p></li></ul><h2 id="可视化debug"><a href="#可视化debug" class="headerlink" title="可视化debug"></a>可视化debug</h2><p>为了在<code>debug</code>的时候可以可视化，即可以在VSCode的调试区查看变量的值和堆栈调用情况等，需要把<code>~\.rustup\toolchains\nightly-x86_64-pc-windows-msvc\lib\rustlib\etc</code>里的文件全部复制粘贴到<code>~\.vscode\extensions\ms-vscode.cpptools-0.11.2\debugAdapters\vsdbg\bin\Visualizers</code></p><h2 id="vscode配置"><a href="#vscode配置" class="headerlink" title=".vscode配置"></a>.vscode配置</h2><p>开一个文件夹，以后都在这个文件夹下用<code>Cargo</code>新建项目。用VSCode打开新建的文件夹，点小蜘蛛再点调试那一栏的小齿轮，添加<code>launch.json</code>配置文件，内容如下</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 使用 IntelliSense 了解相关属性。 </span></span><br><span class="line">    <span class="comment">// 悬停以查看现有属性的描述。</span></span><br><span class="line">    <span class="comment">// 欲了解更多信息，请访问: https://go.microsoft.com/fwlink/?linkid=830387</span></span><br><span class="line">    <span class="attr">&quot;version&quot;</span>: <span class="string">&quot;0.2.0&quot;</span>,</span><br><span class="line">    <span class="attr">&quot;configurations&quot;</span>: [</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="attr">&quot;name&quot;</span>: <span class="string">&quot;(Windows) Launch&quot;</span>,</span><br><span class="line">            <span class="attr">&quot;type&quot;</span>: <span class="string">&quot;cppvsdbg&quot;</span>,</span><br><span class="line">            <span class="attr">&quot;request&quot;</span>: <span class="string">&quot;launch&quot;</span>,</span><br><span class="line">            <span class="attr">&quot;program&quot;</span>: <span class="string">&quot;$&#123;workspaceFolder&#125;/target/debug/$&#123;workspaceFolderBasename&#125;.exe&quot;</span>,</span><br><span class="line">            <span class="attr">&quot;args&quot;</span>: [],</span><br><span class="line">            <span class="attr">&quot;stopAtEntry&quot;</span>: <span class="literal">false</span>,</span><br><span class="line">            <span class="attr">&quot;cwd&quot;</span>: <span class="string">&quot;$&#123;workspaceFolder&#125;&quot;</span>,</span><br><span class="line">            <span class="attr">&quot;preLaunchTask&quot;</span>: <span class="string">&quot;cargo build&quot;</span>,</span><br><span class="line">            <span class="attr">&quot;environment&quot;</span>: [],</span><br><span class="line">            <span class="attr">&quot;externalConsole&quot;</span>: <span class="literal">true</span></span><br><span class="line">        &#125;</span><br><span class="line">    ]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>再点菜单栏的<code>任务</code>-&gt;<code>配置任务</code>-&gt;<code>使用模板创建tasks.json文件</code>，配置内容如下：</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="comment">// See https://go.microsoft.com/fwlink/?LinkId=733558</span></span><br><span class="line">    <span class="comment">// for the documentation about the tasks.json format</span></span><br><span class="line">    <span class="attr">&quot;version&quot;</span>: <span class="string">&quot;2.0.0&quot;</span>,</span><br><span class="line">    <span class="attr">&quot;tasks&quot;</span>: [</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="attr">&quot;type&quot;</span>: <span class="string">&quot;shell&quot;</span>,</span><br><span class="line">            <span class="attr">&quot;label&quot;</span>: <span class="string">&quot;cargo build&quot;</span>,</span><br><span class="line">            <span class="attr">&quot;command&quot;</span>: <span class="string">&quot;cargo&quot;</span>,</span><br><span class="line">            <span class="attr">&quot;args&quot;</span>: [</span><br><span class="line">                <span class="string">&quot;build&quot;</span></span><br><span class="line">            ],</span><br><span class="line">            <span class="attr">&quot;problemMatcher&quot;</span>: [</span><br><span class="line">                <span class="string">&quot;$rustc&quot;</span></span><br><span class="line">            ]</span><br><span class="line">        &#125;</span><br><span class="line">    ]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="具体使用"><a href="#具体使用" class="headerlink" title="具体使用"></a>具体使用</h1><p>然后就和写c\c++一样了，你写完rust代码，在行号左侧设置断点，点调试区的绿色开始键，就会自动帮你<code>cargo build</code>然后运行程序开始调试。</br><br><img src="/images/rust.jpg" alt="成品"><br><strong>Enjoy Rust!</strong></p><h1 id="参考"><a href="#参考" class="headerlink" title="参考"></a><em>参考</em></h1><ol><li><a class="link"   href="https://code.visualstudio.com/docs/editor/variables-reference" >https://code.visualstudio.com/docs/editor/variables-reference<i class="fas fa-external-link-alt"></i></a></li><li><a class="link"   href="https://www.brycevandyk.com/debug-rust-on-windows-with-visual-studio-code-and-the-msvc-debugger/" >https://www.brycevandyk.com/debug-rust-on-windows-with-visual-studio-code-and-the-msvc-debugger/<i class="fas fa-external-link-alt"></i></a></li></ol>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;前言&quot;&gt;&lt;a href=&quot;#前言&quot; class=&quot;headerlink&quot; title=&quot;前言&quot;&gt;&lt;/a&gt;前言&lt;/h1&gt;&lt;p&gt;搞了整整一天的rust+vscode的开发环境，因为rust太新了，再加上Windows用户往往只是开发者中的小众，所以完整的、高质量的环境</summary>
      
    
    
    
    
    <category term="rust" scheme="https://www.zenwill.top/tags/rust/"/>
    
    <category term="vscode" scheme="https://www.zenwill.top/tags/vscode/"/>
    
  </entry>
  
  <entry>
    <title>浅谈Python闭包与装饰器</title>
    <link href="https://www.zenwill.top/2018/05/24/%E6%B5%85%E8%B0%88Python%E9%97%AD%E5%8C%85%E4%B8%8E%E8%A3%85%E9%A5%B0%E5%99%A8/"/>
    <id>https://www.zenwill.top/2018/05/24/%E6%B5%85%E8%B0%88Python%E9%97%AD%E5%8C%85%E4%B8%8E%E8%A3%85%E9%A5%B0%E5%99%A8/</id>
    <published>2018-05-24T20:43:04.000Z</published>
    <updated>2021-07-05T10:36:58.450Z</updated>
    
    <content type="html"><![CDATA[<h1 id="前置知识"><a href="#前置知识" class="headerlink" title="前置知识"></a>前置知识</h1><h2 id="加载、解释与执行"><a href="#加载、解释与执行" class="headerlink" title="加载、解释与执行"></a>加载、解释与执行</h2><p>python是解释型语言、脚本语言，一大特点是一行语句只有需要被执行时才会被编译一次，编译后立即执行。所谓的“需要被执行”指python解释器“阅读”代码的顺序，通常来说是从上往下一行一行“阅读”，也就是编译并执行完了当前行，才会执行下一行；当然也有例外的时候，比如设置了for循环（需要注意的是，假设for循环中有4行代码，循环3次，那么解释器实际上编译了12行），还有就是调用方法（函数），解释器会跳转到方法代码块开始编译执行。观察如下代码：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">fun</span>(<span class="params">a</span>):</span></span><br><span class="line">    a = a ** <span class="number">2</span></span><br><span class="line">    <span class="keyword">return</span> a</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;调用fun方法&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(fun(a))</span><br></pre></td></tr></table></figure><p>这段代码的编译执行顺序是1、4、2、3、5。（提示：函数定义真正被执行的只有def行，函数体中的代码只是以字符串的形式简单地<strong>加载</strong>进了内存，并没有被编译，函数每一次调用的时候才进行一轮编译执行；第五行中出现了函数调用，会先跳转到函数块，执行完函数中的语句拿到返回值后跳转回来执行<code>print</code>）</p><h2 id="一切皆对象"><a href="#一切皆对象" class="headerlink" title="一切皆对象"></a>一切皆对象</h2><p>python中一切皆对象的思想比Java更为彻底。<code>int</code>型等所有变量是对象，函数是对象（类也是对象，这个暂时不做展开）。是对象就会有对象的属性和方法，这里介绍一种很有用的方法，<code>id(对象名)</code>，这个方法会返回对象名所指向对象的地址，利用它可以很好的研究代码，观察各个变量的内存位置。因为一切皆对象，所以python中的一切变量都是引用变量，</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">a = <span class="number">1</span></span><br><span class="line">a = <span class="number">2</span></span><br><span class="line">b = <span class="number">2</span></span><br><span class="line">a = fun</span><br><span class="line"><span class="built_in">print</span>(a(<span class="number">2</span>))</span><br><span class="line"><span class="built_in">print</span>(<span class="built_in">id</span>(a) == <span class="built_in">id</span>(b))</span><br></pre></td></tr></table></figure><p>第一行到第二行，并不是给a变量赋了新的值，而是把a的指向从存放对象int 1的内存地址改到了存放int 2对象的内存地址（事实上python的虚拟机会在程序运行前提前创建好0到256的int对象，于是你会发现代码中id(a)和id(b)是一样的，因为他们都指向同一对象）；函数也是对象，是对象就可以被引用，所以第四行表示给fun函数取了一个新名字a，a从指向2变成了指向fun（注意一下fun和fun()的区别，前者的返回值是fun所指的函数对象，后者的返回值是函数里return的东西）。</p><h1 id="闭包"><a href="#闭包" class="headerlink" title="闭包"></a>闭包</h1><p>有了前置知识就很容易学习、理解闭包了。闭包说白了，就是定义一个外层函数，这个外层函数的功能是定义一个内层函数，并返回这个内层函数对象。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">outer</span>(<span class="params">a</span>):</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">inner</span>(<span class="params">b</span>):</span></span><br><span class="line">        <span class="keyword">return</span> a + b</span><br><span class="line">    <span class="keyword">return</span> inner</span><br><span class="line"></span><br><span class="line">f = outer(<span class="number">1</span>)</span><br><span class="line"><span class="built_in">print</span>(f(<span class="number">2</span>))  <span class="comment">#  输出3</span></span><br></pre></td></tr></table></figure><p>1到4这个结构就称为闭包，第六行的功能就是得到了一个叫f的、可以给传入的一个数字加1并返回的函数。上面代码的执行顺序是1、5（第五行为空）、2(识别到第六行有函数调用，所以先跳转执行函数outer，生成一个inner函数对象，并把inner函数体中的代码载入内存)、4、6、3（通过f找到内存中之前生成的inner函数，执行函数语句）、7。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">f = outer(<span class="number">1</span>)</span><br><span class="line">g = outer(<span class="number">2</span>)</span><br></pre></td></tr></table></figure><p>这里调用了两次outer，实际上定义了两次inner，产生了两个不同的函数对象（内存地址不同，功能也不同，f()是传入的数字返回加1的结果，g()是返回加2的结果）。这也是为什么outer的返回值必须赋给（被引用给）一个变量，因为outer()是可以多次利用的，每次调用都会产生一个新的inner函数对象，那么你就需要不同的名字来区别这些函数，方便下次找到他们，比如上面的f和g（<em>outer函数体外面是不属于inner这个名字的作用域的，所以在outer外通过inner()调用函数是非法的，在python解释器看来，这个位置没有定义过一个叫inner的函数或者变量，所以每次产生的inner对象虽然载入了内存，但是出了outer函数体之后就失去了名字，所以必须被一个外部的变量名引用，下次需要调用它的时候才能明确你到底需要调用内存中哪一个函数</em>）。</br><br>我们再来看看新生成的inner对象载入内存这个过程的一些细节，</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">outer</span>(<span class="params">a</span>):</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">inner</span>(<span class="params">b</span>):</span></span><br><span class="line">        <span class="keyword">return</span> a + b</span><br><span class="line">    a = <span class="number">2</span></span><br><span class="line">    <span class="keyword">return</span> inner</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">f = outer(<span class="number">1</span>)</span><br><span class="line"><span class="built_in">print</span>(f(<span class="number">2</span>))  <span class="comment"># 输出4</span></span><br></pre></td></tr></table></figure><p>这段代码和之前的区别就是<code>def</code>之后加了一句<code>a = 2</code>,结果输出就等于4了。如果你看懂了我之前说得调用和定义、执行和加载的区别，你就会理解定义inner对象的时候只是把<code>return a + b</code>这段字符串载入了内存，并没有进行编译，所以当之后通过<code>f(2)</code>调用，编译并执行return语句的时候就需要去看一下现在的a等于几。但是，我想说的是，通常来说，一个函数内部定义的变量，或者其形参的生命周期在函数结束的时候就结束了，也就是说outer结束的时候，解释器理应已经不认识那个叫a的变量了（和inner的道理一样），或者说内存中已经不存在叫<code>a</code>的变量了，那么当我们在outer外部调用<code>f(2)</code>的时候，解释器在编译return语句的时候去哪里找的a呢？这就是闭包之所以为闭包的特殊之处（如果你去看官方文档的闭包定义，你会觉得不知道它在讲什么，实际上它在说我下面要讲的这个特性），解释器在执行<code>def inner(b):</code>的时候会在内存中开一块内存用于存放新生成的inner函数对象和这个对象的一些成员信息，除了将函数体语句（这里指<code>return a + b</code>）载入这块内存,还会载入变量a（a在是inner中被调用的，但是不是在inner中定义的局部变量），相当于捆绑了一份a给新生成的inner对象，所以当outer结束的时候，a并没有被销毁，而是存活在inner对象的内存中（注意，我这里所说的a是否存活，不是指a所指的那个int对象，不是说int对象存到了inner对象的内存中，这里的a是一个引用，你应该把a理解为一份地址信息，而这个地址就是a所指的int对象的地址，P.S 如果你想学好python，就必须习惯所有的变量名、函数名都只是引用）。<br>现在可以尝试在自己机器上运行下面的代码并理解其输出，</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">outer</span>():</span></span><br><span class="line">    fs = []</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">4</span>):  <span class="comment"># i是整数类型 存在预留缓存池中</span></span><br><span class="line">        <span class="function"><span class="keyword">def</span> <span class="title">inner</span>():</span></span><br><span class="line">            <span class="built_in">print</span>(<span class="string">&quot;i&quot;</span>, <span class="built_in">id</span>(i))</span><br><span class="line">            <span class="built_in">print</span>(<span class="string">&quot;func&quot;</span>, <span class="built_in">id</span>(inner))  <span class="comment"># 这里的inner的引用每次定义都会重新覆盖</span></span><br><span class="line">            <span class="keyword">return</span> i</span><br><span class="line">        fs.append(inner)</span><br><span class="line">        inner()</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&quot;*&quot;</span> * <span class="number">30</span>)</span><br><span class="line">    <span class="keyword">for</span> j <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">4</span>):</span><br><span class="line">        <span class="comment"># print(id(fs[j]), fs[j]())</span></span><br><span class="line">        <span class="built_in">print</span>(fs[j](), <span class="built_in">id</span>(fs[j]))  </span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;*&quot;</span> * <span class="number">30</span>)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">4</span>):</span><br><span class="line">        <span class="built_in">print</span>(<span class="built_in">id</span>(fs[i]), fs[i]())</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;*&quot;</span> * <span class="number">30</span>)</span><br><span class="line">    <span class="keyword">return</span> fs</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">f1, f2, f3, f4 = outer()</span><br><span class="line"><span class="built_in">print</span>(f1(), f2(), f3(), f4())</span><br></pre></td></tr></table></figure><h1 id="装饰器"><a href="#装饰器" class="headerlink" title="装饰器"></a>装饰器</h1><p>先理解<code>@</code>的作用，然后再理解通常意义上的装饰器。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">funC</span>():</span></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&#x27;C&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">funA</span>(<span class="params">arg</span>):</span></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&#x27;A&#x27;</span>)</span><br><span class="line">    <span class="keyword">return</span> funC</span><br><span class="line"></span><br><span class="line"><span class="meta">@funA</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">funB</span>():</span></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">&#x27;B&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(funB)</span><br></pre></td></tr></table></figure><p>funA可以称为一个装饰器（通常意义上是装饰器应该写成一个闭包），它的特征是包含一个参数（这个参数会指向被修饰的函数对象，这里arg指向funB），返回一个函数对象（闭包形式中通常返回的是inner函数对象，这里返回了funC）；<code>@funA</code>的作用就是</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">funB = funA(funB)  <span class="comment"># 以后调用funB(),就不是调用的你自己定义的funB函数了，它已经指向了装饰器返回的那个函数对象，也就是funC</span></span><br></pre></td></tr></table></figure><p>不是说能直接这样替换，我只是想意会一下@的作用，同时我希望你能注意到<code>@funA</code>不是定义或者加载，这里是真的跳转过去执行了一次funA，所以如果你在输出中看到了A，那么这个A是解释器在处理<code>@funA</code>时产生的，而不是在后面处理<code>print(funB)</code>时产生的。如果你对这些过程还有疑问，可以在各个位置添加<code>print(id(函数名))</code>来观察整个流程中的函数名指向的变化。</br><br>下面通过一个比较简单的例子来解释我们为什么需要装饰器，并引出通常意义上的装饰器。假设你的同事定义了一系列函数，比如</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">f</span>(<span class="params">x</span>):</span></span><br><span class="line">    <span class="keyword">return</span> x + <span class="number">1</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">g</span>(<span class="params">x</span>):</span></span><br><span class="line">    <span class="keyword">return</span> x + <span class="number">2</span></span><br></pre></td></tr></table></figure><p>你的老板说，“我希望每次调用一个函数都给我打印一下我调用的这个函数的名字，这样我们的程序变大变复杂了之后，我可以通过看输出知道我们的程序运行过程中调用了哪些函数，调用的次数和顺序，而不用去翻代码”，然后他把这个任务交给了你。最笨的方法当然是在每个函数的定义里加一句<code>print(函数名.__name__)</code>，（这里的__name__是函数对象的一个成员变量，其内容是函数名的字符串，这里的函数名是指def的时候用的那个名字，你可以在之前闭包代码的inner里加上这一句看看会打印出什么）,问题是你同事定义的那些函数又多又臭又长，你根本不想看，你也不能叫你同事去加，一方面现在只是加一行print代码比较简单，如果老板以后要求更复杂的功能呢，另一方面如果找同事去做就能解决问题，老板要你干嘛（<strong>另外有一个原则性问题，我们不应该轻易去修改已经写好的代码块，因为即使那些代码是你自己写的，一段时间之后，你也可能忘掉其中的一些逻辑细节，轻易修改会导致bug</strong>）。所以，这个时候最好的办法就是写一个装饰器：</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">outer</span>(<span class="params">fun</span>):</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">inner</span>(<span class="params">b</span>):</span></span><br><span class="line">        <span class="built_in">print</span>(fun.__name__)</span><br><span class="line">        <span class="keyword">return</span> fun(b)</span><br><span class="line">    <span class="keyword">return</span> inner</span><br></pre></td></tr></table></figure><p>然后你只需要跟你的同事说，在他写的每一句def前面加一句<code>@outer</code>。例如</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@outer</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">f</span>(<span class="params">x</span>):</span></span><br><span class="line">    <span class="keyword">return</span> x + <span class="number">1</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@outer</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">g</span>(<span class="params">x</span>):</span></span><br><span class="line">    <span class="keyword">return</span> x + <span class="number">2</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(f(<span class="number">1</span>))</span><br></pre></td></tr></table></figure><p>这样调用f的时候，实际上是调用了f新指向的那个inner，而执行的时候fun就是被修饰前的那个f，就是同事def的那个函数，解释器会执行<code>print(fun.__name__)</code>（实现了老板要求你加的功能），然后执行<code>return fun(b)</code>（保留了你同事原来写的函数的功能），括号里的内容显示了这个叫装饰器的东西的优点。</br><br>讲到这里，你已经基本理解了装饰器的概念、原理及优点。如果你上网查装饰器，你还会发现其他许多借助装饰器实现的骚操作，我这里只介绍了最简单的版本，但是你理解了我讲的这些之后，再去看那些更秀的操作，就不会一头雾水了。这里我在多讲一个内容，就是装饰器有其他参数的情况。</br><br>你的公司100多个员工，每个人都写了一批函数，老板说，“我还希望调用一个函数的时候可以打印出这个函数是谁写的，这样出bug的时候，我可以知道该扣哪个人的工资”，你已经会写装饰器了，于是你轻松完成了这个任务，</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">outer</span>(<span class="params">name</span>):</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">mid</span>(<span class="params">fun</span>):</span></span><br><span class="line">        <span class="function"><span class="keyword">def</span> <span class="title">inner</span>(<span class="params">b</span>):</span></span><br><span class="line">            <span class="built_in">print</span>(name)</span><br><span class="line">            <span class="built_in">print</span>(fun.__name__)</span><br><span class="line">            <span class="keyword">return</span> fun(b)</span><br><span class="line">        <span class="keyword">return</span> inner</span><br><span class="line">    <span class="keyword">return</span> mid</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">@outer(<span class="params"><span class="string">&#x27;szw&#x27;</span></span>)</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">f</span>(<span class="params">x</span>):</span></span><br><span class="line">    <span class="keyword">return</span> x + <span class="number">1</span></span><br></pre></td></tr></table></figure><p>然后你和所有人说，在你们的函数前都加上<code>@outer(&#39;你的名字&#39;)</code>吧。现在我们用前面所学的所有知识来理解一下这个装饰器，以检测你是不是真的理解了前面的东西。乍一看这个装饰器写了三层函数，我刚看到的时候也是一脸懵逼，但是这实际上这并不是什么新规则，用我们前面讲的逻辑就能理解它了。正常的装饰标志是<code>@outer</code>这样的对吧，@后面应该写一个函数对象，而不是调用一个函数（参看前面说的关于fun和fun()的区别），那么<code>@outer(&#39;szw&#39;)</code>这里@后面为什么写了一个调用呢？对的，这里的确是调用了，所以你看看调用的返回值是什么，是一个函数对象mid，那这样就可以理解了——调用执行了<code>outer(&#39;szw&#39;)</code>后返回了一个和<code>name=&#39;szw&#39;</code>这个局部变量捆绑了的函数对象mid（参看前面讲的闭包的特性），然后执行<code>@mid</code>,再然后就和我们之前说的普通的装饰器的流程一样了。</br><br>这个故事告诉我们，语言的特性（语法规则）能为程序员提供多大的便利是一回事，程序员能在规则之上写出怎样的骚操作就是另一个故事了，这取决于一个程序员灵活利用规则的造诣。而<strong>造诣=天分+努力</strong>，如果你不够聪明，就需要多看别人的代码，模仿并理解别人的骚操作。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;前置知识&quot;&gt;&lt;a href=&quot;#前置知识&quot; class=&quot;headerlink&quot; title=&quot;前置知识&quot;&gt;&lt;/a&gt;前置知识&lt;/h1&gt;&lt;h2 id=&quot;加载、解释与执行&quot;&gt;&lt;a href=&quot;#加载、解释与执行&quot; class=&quot;headerlink&quot; title=&quot;加</summary>
      
    
    
    
    
    <category term="python" scheme="https://www.zenwill.top/tags/python/"/>
    
    <category term="algorithm" scheme="https://www.zenwill.top/tags/algorithm/"/>
    
  </entry>
  
  <entry>
    <title>HTML基础</title>
    <link href="https://www.zenwill.top/2018/03/21/HTML%E5%9F%BA%E7%A1%80/"/>
    <id>https://www.zenwill.top/2018/03/21/HTML%E5%9F%BA%E7%A1%80/</id>
    <published>2018-03-21T13:02:44.000Z</published>
    <updated>2021-07-05T10:36:58.450Z</updated>
    
    <content type="html"><![CDATA[<h1 id="HTML基础"><a href="#HTML基础" class="headerlink" title="HTML基础"></a>HTML基础</h1><h2 id="语言简介"><a href="#语言简介" class="headerlink" title="语言简介"></a>语言简介</h2><p><strong>HTML</strong>（Hypertext Markup Language, <em>超文本标记语言</em>)，说白了就是网页的源码，包含、描述了网页中将要展示的文本、图片等信息，同时浏览器可以理解为这种格式的代码的编译器。因此HTML是学习网页设计要掌握的最基础知识（后续要掌握的知识有CSS、JavaScript）。本文旨在记录一些基础的语法，或者说元素。</p><h2 id="基础知识"><a href="#基础知识" class="headerlink" title="基础知识"></a>基础知识</h2><p>如果你打开一个网页，然后点选浏览器选项中的查看源码，那就会看到一坨带有<code>&lt;xxx&gt;</code>这种类似符号的代码，这种记号称之为<strong>tags</strong>，用来组织网页信息。<code>&lt;xxx yy=&quot;zz&quot;&gt;</code> yy是xxx tag的一个属性（attributes），用来增加一些额外的信息，比如tags所包含文字的字体；“zz”是属性值，例如zz是字体名称。只有“true”和“false”两个值的属性（称为Boolean attributes）在只写属性名的时候默认值为true，不写该属性，其值默认为false。</p><h3 id="基本tags"><a href="#基本tags" class="headerlink" title="基本tags"></a>基本tags</h3><p><code>&lt;html&gt;&lt;/html&gt;</code> 表示html的开始和结束，也就是所有代码都要放在这两个tags中间。<code>&lt;html lang=&quot;en-GB&quot;&gt;</code>lang属性表示网页的语言。</p><p><code>&lt;head&gt;&lt;/head&gt;</code> 包含head section，其中的内容不在页面直接显示</p><p><code>&lt;title&gt;&lt;/title&gt;</code> 在head section中，包含浏览器打开网页时的标签页名</p><p><code>&lt;body&gt;&lt;/body&gt;</code> 包含body section，其中的内容在网页中直接显示</p><p><code>&lt;!DOCTYPE html&gt;</code> 一般写在文档的最前面，用来告诉浏览器所用的代码版本是HTML5</p><p><code>&lt;h1&gt;&lt;/h1&gt;</code>、<code>&lt;h2&gt;&lt;/h2&gt;</code> 包含标题，按数字分层</p><p><code>&lt;p&gt;&lt;/p&gt;</code> 包含一个段落</p><p><code>&lt;br&gt;</code> 换行符。这个tag不像上面几个那样是成对的，因为它不需要包含内容，这样的tag叫empty tag</p><p><code>&lt;em&gt;&lt;/em&gt;</code> 被其包含的文字会显示斜体</p><p><code>&lt;strong&gt;&lt;/strong&gt;</code> 被其包含的文字会显示粗体</p><p><code>&lt;hr&gt;</code> 分割线</p><p><code>&lt;!--F**K THE WIRLD--&gt;</code> 这个是注释</p><h3 id="超链接（hyperlinks）与多媒体"><a href="#超链接（hyperlinks）与多媒体" class="headerlink" title="超链接（hyperlinks）与多媒体"></a>超链接（hyperlinks）与多媒体</h3><p><code>&lt;a&gt;&lt;/a&gt;</code> 其中的文字或图片等会变成超链接。<code>&lt;a href=&quot;https://*****&quot; target=&quot;_blank&quot;&gt;</code> href属性为超链接所指路径或网址;target属性为打开新网页的方式，属性值“_blank”打开新标签页</p><p><code>&lt;img src=&quot;&quot; width=&quot;200&quot; hight=&quot;135&quot; alt=&quot;oops!&quot;&gt;</code> 图片。src属性为图片的地址;alt为图片路径失效时显示的图片。通过tags的嵌套<code>&lt;a&gt;&lt;img&gt;&lt;/a&gt;</code>可以实现将图片变成超链接</p><p><code>&lt;iframe&gt;&lt;/iframe&gt;</code> inline frame,网页中的一个子网页或者说框架，利用YouTube可以实现视频的插入 <code>&lt;iframe width=&quot;200&quot; hight=&quot;135&quot; src=&quot;https://www.youtube.com/embed/****&quot;&gt;&lt;/iframe&gt;</code></p><p><code>&lt;video src=&quot;&quot; controls=&quot;true&quot;&gt;</code> 插入视频的另一种方式。这里src是视频文件的地址；control为“true”时显示视频的播放和暂停按钮</p><p><code>&lt;audio&gt;</code> 插入音频。用法与video类似</p><h3 id="列表（list）与表格（table）"><a href="#列表（list）与表格（table）" class="headerlink" title="列表（list）与表格（table）"></a>列表（list）与表格（table）</h3><p><code>&lt;ol&gt;&lt;/ol&gt;</code> 有序号的列表。type属性表示编号的类型，如默认为1.2.3.,值“A”表示按A.B.C.排列，其他属性值还有“a”，希腊字母“I”等；属性reversed表示是否编号按降序排列，是一个Boolean attributes</p><p><code>&lt;ul&gt;&lt;/ul&gt;</code> 没有序号的列表</p><p><code>&lt;li&gt;&lt;/li&gt;</code> 列表中的一项。嵌在ol或ul中</p><p><code>&lt;table&gt;&lt;/table&gt;</code> 有行和列的表格。border属性值有“1”和“0”，表示是否加边框</p><p><code>&lt;tr&gt;&lt;/tr&gt;</code> 表格中的一行。嵌在table tags中</p><p><code>&lt;td&gt;&lt;/td&gt;</code> 一行中的一格。嵌在tr tags中</p><p><code>&lt;th&gt;&lt;/th&gt;</code> 表头所占的一格。通常嵌在第一组tr中。属性colspan表示在这个标题下拆分多少列</p><p>###风格（style）<br>基本所有的tags（或者称为HTML element）都有一个style属性，用来设定大小、颜色等等，以达到美好效果，如<code>&lt;body style=&quot;background_color:gray&quot;&gt;</code>可以设定网页背景色。style的值称为style declaration，是用CSS格式写的。CSS是一种用来设计网页显示效果的语言，格式是<code>property:value;</code>，这里value是多个单词时，用单引号括起来。property除了背景颜色，还有文字颜色<code>color</code>、字体<code>font-family</code>、字体大小<code>font-size</code>(值为像素px或百分比%)、文字位置<code>text-align</code>（如值center使文字居中）<br>可以用<code>&lt;div&gt;&lt;/div&gt;</code>将代码块括起来，相当于在网页中圈了一块区域（section），然后用style属性对这块区域进行设计。而<code>&lt;span&gt;&lt;/span&gt;</code>可以用来截取文字中的一部分进行style</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;HTML基础&quot;&gt;&lt;a href=&quot;#HTML基础&quot; class=&quot;headerlink&quot; title=&quot;HTML基础&quot;&gt;&lt;/a&gt;HTML基础&lt;/h1&gt;&lt;h2 id=&quot;语言简介&quot;&gt;&lt;a href=&quot;#语言简介&quot; class=&quot;headerlink&quot; title=&quot;语</summary>
      
    
    
    
    
    <category term="html" scheme="https://www.zenwill.top/tags/html/"/>
    
  </entry>
  
  <entry>
    <title>猜数游戏</title>
    <link href="https://www.zenwill.top/2018/03/16/%E7%8C%9C%E6%95%B0%E6%B8%B8%E6%88%8F/"/>
    <id>https://www.zenwill.top/2018/03/16/%E7%8C%9C%E6%95%B0%E6%B8%B8%E6%88%8F/</id>
    <published>2018-03-16T20:27:56.000Z</published>
    <updated>2021-07-05T10:36:58.450Z</updated>
    
    <content type="html"><![CDATA[<h1 id="猜数游戏"><a href="#猜数游戏" class="headerlink" title="猜数游戏"></a>猜数游戏</h1><h2 id="问题概述"><a href="#问题概述" class="headerlink" title="问题概述"></a>问题概述</h2><p>猜一个没有重复数字的四位数，每猜一次会反馈2个数字c和e，c表示你所猜的四位数中有几个数字在答案中出现，e表示出现的这几个数字有几个在四位数中的位置与答案一致<em>（为了使得问题有良好的对称性，第四位是0的情况，即三位数，也是允许的）</em>。要求写一个程序进行猜数，以尽可能少的次数猜到答案。</p><h2 id="思路"><a href="#思路" class="headerlink" title="思路"></a>思路</h2><p>设所有可能的数的集合为<strong>S</strong>，则答案是其中随机的一个元素。游戏的本质是根据反馈信息不断缩小<strong>S</strong>，直到找到答案。程序的任务就是根据已知信息（包括<strong>S</strong>的状态和反馈信息），作出猜哪个数的决策。因此，要提高猜数效率，有两点可以入手：<br><strong>1. 尽可能的利用反馈所带来的信息。</strong>不要浪费信息量，使得反馈所携带的所有信息都被程序用来减小集合S，即剔除不满足反馈信息的元素。方法是将集合中的每一个元素假设为答案，然后与你猜的数对比，如果计算出的反馈和实际得到的不同，说明该元素不是答案，换句话说，该元素已被反馈所携带的信息排除。每一次得到反馈后，都马上排除所有不符合信息的元素，即最大程度的利用了反馈信息。<br><strong>2. 每次猜可以最大程度削减S的那个数。</strong>当程序要决定应该猜哪个数时，对<strong>S</strong>中的每一个元素计算：猜该元素时，出现每种可能反馈时能排除的元素个数（程序中实际计算了能剩下的元素的个数）。然后比较所有元素削减<strong>S</strong>的程度。比较的方法有两个标准，对于每一个元素，取所有反馈对应剩下个数中最大值作为该猜元素后<strong>S</strong>还剩下多少的估计值（即最坏情况），另一种是取所有反馈情况的平均情况。使得<strong>S</strong>中元素剩下个数最少的那个数就是要猜的数。<br>我的程序里两种标准都尝试了，并多次重复实验计算了平均猜数次数。神奇的是两种标准得到的平均次数是一样的，都是5.59左右。我觉得这个现象应该是可以理论证明的，但是我还没有想出来。先把疑问记录在这里，以后有了想法再补充吧。</p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br><span class="line">131</span><br><span class="line">132</span><br><span class="line">133</span><br><span class="line">134</span><br><span class="line">135</span><br><span class="line">136</span><br><span class="line">137</span><br><span class="line">138</span><br><span class="line">139</span><br><span class="line">140</span><br><span class="line">141</span><br><span class="line">142</span><br><span class="line">143</span><br><span class="line">144</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> random <span class="keyword">import</span> randint <span class="keyword">as</span> ri</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">feedback</span>(<span class="params">num_a, num_b</span>):</span></span><br><span class="line">    str_a = <span class="built_in">str</span>(num_a)</span><br><span class="line">    str_b = <span class="built_in">str</span>(num_b)</span><br><span class="line">    c = <span class="number">0</span></span><br><span class="line">    e = <span class="number">0</span></span><br><span class="line">    <span class="keyword">if</span> <span class="built_in">len</span>(str_a) == <span class="number">3</span>:</span><br><span class="line">        str_a = <span class="string">&quot;0&quot;</span> + str_a</span><br><span class="line">    <span class="keyword">if</span> <span class="built_in">len</span>(str_b) == <span class="number">3</span>:</span><br><span class="line">        str_b = <span class="string">&quot;0&quot;</span> + str_b</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">4</span>):</span><br><span class="line">        <span class="keyword">for</span> j <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">4</span>):</span><br><span class="line">            <span class="keyword">if</span> str_a[i] == str_b[j]:</span><br><span class="line">                c += <span class="number">1</span></span><br><span class="line">                <span class="keyword">if</span> i == j:</span><br><span class="line">                    e += <span class="number">1</span></span><br><span class="line">    <span class="keyword">return</span> (c, e)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">update_set</span>(<span class="params">list_old, last, feed</span>):</span></span><br><span class="line">    list_new = []</span><br><span class="line">    num = <span class="built_in">len</span>(list_old)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(num):</span><br><span class="line">        <span class="keyword">if</span> feedback(list_old[i], last) == feed:</span><br><span class="line">            list_new.append(list_old[i])</span><br><span class="line">    <span class="keyword">return</span> list_new</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">decision_1</span>(<span class="params">list_new</span>):</span></span><br><span class="line">    choise = <span class="number">0</span></span><br><span class="line">    <span class="keyword">for</span> n <span class="keyword">in</span> list_new:</span><br><span class="line">        min_n = <span class="number">10000</span></span><br><span class="line">        dict_f = &#123;&#125;</span><br><span class="line">        <span class="keyword">for</span> m <span class="keyword">in</span> list_new:</span><br><span class="line">            feed = feedback(n, m)</span><br><span class="line">            <span class="keyword">if</span> feed <span class="keyword">not</span> <span class="keyword">in</span> dict_f:</span><br><span class="line">                dict_f[feed] = <span class="number">1</span></span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                dict_f[feed] += <span class="number">1</span></span><br><span class="line">        temp = <span class="built_in">sum</span>(dict_f.values()) / <span class="built_in">len</span>(dict_f)</span><br><span class="line">        <span class="keyword">if</span> min_n &gt; temp:</span><br><span class="line">            min_n = temp</span><br><span class="line">            choise = n</span><br><span class="line">    <span class="keyword">return</span> choise</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">decision_2</span>(<span class="params">list_new</span>):</span></span><br><span class="line">    choise = <span class="number">0</span></span><br><span class="line">    <span class="keyword">for</span> n <span class="keyword">in</span> list_new:</span><br><span class="line">        min_n = <span class="number">10000</span></span><br><span class="line">        dict_f = &#123;&#125;</span><br><span class="line">        <span class="keyword">for</span> m <span class="keyword">in</span> list_new:</span><br><span class="line">            feed = feedback(n, m)</span><br><span class="line">            <span class="keyword">if</span> feed <span class="keyword">not</span> <span class="keyword">in</span> dict_f:</span><br><span class="line">                dict_f[feed] = <span class="number">1</span></span><br><span class="line">            <span class="keyword">else</span>:</span><br><span class="line">                dict_f[feed] += <span class="number">1</span></span><br><span class="line">        temp = <span class="built_in">max</span>(dict_f.values())</span><br><span class="line">        <span class="keyword">if</span> min_n &gt; temp:</span><br><span class="line">            min_n = temp</span><br><span class="line">            choise = n</span><br><span class="line">    <span class="keyword">return</span> choise</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">list_ini = []  <span class="comment"># 初始数据集</span></span><br><span class="line"><span class="keyword">for</span> i1 <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">10</span>):</span><br><span class="line">    <span class="keyword">for</span> i2 <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">10</span>):</span><br><span class="line">        <span class="keyword">if</span> i1 == i2:</span><br><span class="line">            <span class="keyword">continue</span></span><br><span class="line">        <span class="keyword">for</span> i3 <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">10</span>):</span><br><span class="line">            <span class="keyword">if</span> i3 == i1 <span class="keyword">or</span> i3 == i2:</span><br><span class="line">                <span class="keyword">continue</span></span><br><span class="line">            <span class="keyword">for</span> i4 <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">10</span>):</span><br><span class="line">                <span class="keyword">if</span> i4 == i1 <span class="keyword">or</span> i4 == i2 <span class="keyword">or</span> i4 == i3:</span><br><span class="line">                    <span class="keyword">continue</span></span><br><span class="line">                list_ini.append(i1 * <span class="number">1000</span> + i2 * <span class="number">100</span> + i3 * <span class="number">10</span> + i4)</span><br><span class="line">n = <span class="number">10</span> * <span class="number">9</span> * <span class="number">8</span> * <span class="number">7</span></span><br><span class="line"></span><br><span class="line">sum_1 = <span class="number">0</span></span><br><span class="line">sum_2 = <span class="number">0</span></span><br><span class="line">t = <span class="number">10000</span></span><br><span class="line">dict_choise = &#123;(<span class="number">0</span>, <span class="number">0</span>): <span class="number">9876</span>, (<span class="number">1</span>, <span class="number">0</span>): <span class="number">9872</span>, (<span class="number">1</span>, <span class="number">1</span>): <span class="number">9873</span>, (<span class="number">2</span>, <span class="number">0</span>): <span class="number">9832</span>, (<span class="number">2</span>, <span class="number">1</span>): <span class="number">9821</span>, (<span class="number">2</span>, <span class="number">2</span>): <span class="number">9823</span>, (<span class="number">3</span>, <span class="number">0</span>): <span class="number">9312</span>, (<span class="number">3</span>, <span class="number">1</span>): <span class="number">9321</span>, (<span class="number">3</span>, <span class="number">2</span>): <span class="number">9123</span>, (<span class="number">3</span>, <span class="number">3</span>): <span class="number">9023</span>, (<span class="number">4</span>, <span class="number">0</span>): <span class="number">3210</span>, (<span class="number">4</span>, <span class="number">1</span>): <span class="number">3120</span>, (<span class="number">4</span>, <span class="number">2</span>): <span class="number">3021</span>, (<span class="number">4</span>, <span class="number">3</span>): <span class="number">0</span>, (<span class="number">4</span>, <span class="number">4</span>): <span class="number">1023</span>&#125;</span><br><span class="line"><span class="comment"># dict_choise_2 = &#123;&#125;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># for c in range(5):</span></span><br><span class="line"><span class="comment">#     for e in range(c + 1):</span></span><br><span class="line"><span class="comment">#         list_temp = update_set(list_ini, 1023, (c, e))</span></span><br><span class="line"><span class="comment">#         choise_temp_1 = decision_1(list_temp)</span></span><br><span class="line"><span class="comment">#         choise_temp_2 = decision_2(list_temp)</span></span><br><span class="line"><span class="comment">#         dict_choise_1[(c, e)] = choise_temp_1</span></span><br><span class="line"><span class="comment">#         dict_choise_2[(c, e)] = choise_temp_2</span></span><br><span class="line"><span class="comment"># print(dict_choise_1)</span></span><br><span class="line"><span class="comment"># print(dict_choise_2)</span></span><br><span class="line"><span class="keyword">for</span> j <span class="keyword">in</span> <span class="built_in">range</span>(t):  <span class="comment"># 循环测试,调整数字以控制测试次数</span></span><br><span class="line">    ans_index = ri(<span class="number">0</span>, n - <span class="number">1</span>)  <span class="comment"># 在初始数据集中随机取一个数字作为答案</span></span><br><span class="line">    ans = list_ini[ans_index]</span><br><span class="line">    <span class="comment"># print(&quot;Case&quot;, j, &quot;:&quot;, ans)</span></span><br><span class="line">    <span class="comment"># choise = 1023</span></span><br><span class="line">    i = <span class="number">0</span></span><br><span class="line">    feed_ini = feedback(ans, <span class="number">1023</span>)</span><br><span class="line">    list_one = update_set(list_ini, <span class="number">1023</span>, feed_ini)</span><br><span class="line">    list_new = list_one</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">1</span>, <span class="number">50</span>):</span><br><span class="line">        <span class="keyword">if</span> i == <span class="number">1</span>:</span><br><span class="line">            choise = dict_choise[feed_ini]</span><br><span class="line">        feed = feedback(ans, choise)</span><br><span class="line">        <span class="comment"># print(choise, feed)</span></span><br><span class="line">        <span class="comment"># c = int(input())</span></span><br><span class="line">        <span class="comment"># e = int(input())</span></span><br><span class="line">        <span class="keyword">if</span> feed[<span class="number">1</span>] == <span class="number">4</span>:</span><br><span class="line">            <span class="comment"># print(&quot;win!&quot;)</span></span><br><span class="line">            i += <span class="number">1</span></span><br><span class="line">            <span class="keyword">break</span></span><br><span class="line">        <span class="comment"># feed = (c, e)</span></span><br><span class="line">        list_new = update_set(list_new, choise, feed)</span><br><span class="line">        choise = decision_1(list_new)</span><br><span class="line">    sum_1 += i</span><br><span class="line">    <span class="comment"># choise = 1023</span></span><br><span class="line">    list_new = list_one</span><br><span class="line">    i = <span class="number">0</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">1</span>, <span class="number">50</span>):</span><br><span class="line">        <span class="keyword">if</span> i == <span class="number">1</span>:</span><br><span class="line">            choise = dict_choise[feed_ini]</span><br><span class="line">        feed = feedback(ans, choise)</span><br><span class="line">        <span class="comment"># print(choise, feed)</span></span><br><span class="line">        <span class="comment"># c = int(input())</span></span><br><span class="line">        <span class="comment"># e = int(input())</span></span><br><span class="line">        <span class="keyword">if</span> feed[<span class="number">1</span>] == <span class="number">4</span>:</span><br><span class="line">            <span class="comment"># print(&quot;win!&quot;)</span></span><br><span class="line">            i += <span class="number">1</span></span><br><span class="line">            <span class="keyword">break</span></span><br><span class="line">        <span class="comment"># feed = (c, e)</span></span><br><span class="line">        list_new = update_set(list_new, choise, feed)</span><br><span class="line">        choise = decision_2(list_new)</span><br><span class="line">    sum_2 += i</span><br><span class="line">    <span class="built_in">print</span>(j)</span><br><span class="line"><span class="built_in">print</span>(sum_1 / t)</span><br><span class="line"><span class="built_in">print</span>(sum_2 / t)</span><br><span class="line"><span class="comment"># if i == 20:</span></span><br><span class="line"><span class="comment"># print(&quot;fail!&quot;)</span></span><br><span class="line"><span class="comment"># else:</span></span><br><span class="line"><span class="comment"># print(&quot;tried&quot;, i, &quot;times\n&quot;)</span></span><br></pre></td></tr></table></figure>]]></content>
    
    
      
      
    <summary type="html">&lt;h1 id=&quot;猜数游戏&quot;&gt;&lt;a href=&quot;#猜数游戏&quot; class=&quot;headerlink&quot; title=&quot;猜数游戏&quot;&gt;&lt;/a&gt;猜数游戏&lt;/h1&gt;&lt;h2 id=&quot;问题概述&quot;&gt;&lt;a href=&quot;#问题概述&quot; class=&quot;headerlink&quot; title=&quot;问题概述&quot;&gt;&lt;/a</summary>
      
    
    
    
    
    <category term="python" scheme="https://www.zenwill.top/tags/python/"/>
    
    <category term="algorithm" scheme="https://www.zenwill.top/tags/algorithm/"/>
    
  </entry>
  
</feed>
